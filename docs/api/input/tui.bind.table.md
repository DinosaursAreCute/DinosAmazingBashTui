### `tui.bind.table`

```bash
tui.bind.table
```

Builds the same rows as [`tui.bind.list`](/api/input/tui.bind.list.html) as an aligned, framed text table in the variable `_TBL`.

**Notes**

- Fork-free; show it with `tui.output PANE "$_TBL"`.
