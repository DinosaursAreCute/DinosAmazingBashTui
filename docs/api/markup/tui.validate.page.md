### `tui.validate.page`

```bash
tui.validate.page FILE
```

Validates one page, adding to the current findings. Ids are checked per page.

**Notes**

- Use [`tui.validate.files`](/api/markup/tui.validate.files.html), which resets the findings first.
