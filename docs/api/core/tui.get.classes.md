### `tui.get.classes`

```bash
tui.get.classes
```

Prints every key the loaded theme defines, sorted, one per line.

**Notes**

- Pseudo-states appear as `CLASS_focus`, `CLASS_hover`, ...
- Fork-free alternative: [`tui.class.names`](/api/style/tui.class.names.html).
