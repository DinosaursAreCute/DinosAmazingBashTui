### `tui.select.set`

```bash
tui.select.set ID ITEM...
```

Replaces the options. The current value stays selected when it is among the new options.

**Notes**

- A new select has no value; call [`tui.select.pick`](/api/widgets/tui.select.pick.html) to preselect one.
