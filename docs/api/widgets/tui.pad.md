### `tui.pad`

```bash
tui.pad ID [HPAD] [VPAD]
```

Sets blank columns and rows around one widget. An empty value is left unchanged.

**Notes**

- For padding inside a pane use [`tui.pane_pad`](/api/core/tui.pane_pad.html).
