### `tui.scan.run_spin`

```bash
tui.scan.run_spin PATH [MSG] [DEEP]
```

Runs [`tui.scan.run`](/api/apps/tui.scan.run.html) behind a spinner (a plain message when stdout is not a terminal). Sets the same variables.
