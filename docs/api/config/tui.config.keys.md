### `tui.config.keys`

```bash
tui.config.keys
```

Prints every stored key, sorted, one per line.

**Output:** one key per line; nothing when the store is empty.

**See also:** [`tui.config.get`](/api/config/tui.config.get.html)
